graph TD
    %% 客户端
    A[患者客户端] 
    B[医生客户端]
    C[管理员客户端]

    %% 服务端部分
    D[服务端<br/>（不可信 App）<br/>网络处理 · 任务转发 · 加密数据存储]
    E[可信 Enclave (Intel SGX)<br/>-----------------------------<br/>1. 远程证明服务<br/>2. 身份认证 (公私钥管理)<br/>3. 访问控制逻辑<br/>4. 病历数据明文处理 (CRUD)<br/>5. 数据加解密 (使用 Enclave 密钥)]

    %% 管理员服务器（你图中右上角单独画的）
    F[管理员服务器]

    %% 连接关系
    A -->|TLS 加密通信| D
    B -->|TLS 加密通信| D
    C -->|TLS 加密通信| D
    D -->|ECall / OCall<br/>Switchless| E
    E -->|加密数据存储| D
    D <.->|管理接口| F

    %% 样式美化
    classDef client fill:#e1f5fe,stroke:#333,stroke-width:2px,rx:10px,ry:10px;
    classDef untrusted fill:#fff2e8,stroke:#d4380d,stroke-width:3px,rx:15px,ry:15px;
    classDef trusted fill:#f6ffed,stroke:#389e0d,stroke-width:4px,rx:15px,ry:15px;
    classDef admin fill:#f9f0ff,stroke:#531dab,stroke-width:2px;

    class A,B,C client
    class D untrusted
    class E trusted
    class F admin

    %% Enclave 用明显边框区分可信/不可信区
    subgraph 服务端主机
        D
        E
    end

    style 服务端主机 fill:none,stroke:#888,stroke-dasharray: 5 5